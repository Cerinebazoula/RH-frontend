/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      // put here your custom theme (colors, screens, etc.)
    },
  },
  plugins: [
    // put here your custom plugins (typography, forms, etc.)
  ],
};
